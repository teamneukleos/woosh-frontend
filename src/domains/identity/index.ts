/**
 * Identity & access domain
 * Auth, membership, permissions, account security.
 */
export { hasPermission, permissionsForRole } from "@/lib/permissions";
export type { Permission } from "@/lib/permissions";
